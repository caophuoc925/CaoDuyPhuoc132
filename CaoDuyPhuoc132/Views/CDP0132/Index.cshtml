@model IEnumerable<CaoDuyPhuoc132.Models.CDP0132>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CDPName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CDPGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CDPName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CDPGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CDPId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CDPId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CDPId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
